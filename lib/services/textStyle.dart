import 'package:flutter/material.dart';

class Header1 extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const Header1({
    this.color = Colors.black,
    this.fontWeight = FontWeight.bold,
    this.size = 20,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class TitleSaham1 extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const TitleSaham1({
    this.color = Colors.black,
    this.fontWeight = FontWeight.bold,
    this.size = 14,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class SubtitleSaham1 extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const SubtitleSaham1({
    this.color = Colors.grey,
    this.fontWeight = FontWeight.w600,
    this.size = 11,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class Parent extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const Parent({
    this.color = Colors.black,
    this.fontWeight = FontWeight.bold,
    this.size = 15,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class LihatSemua extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const LihatSemua({
    this.color = Colors.grey,
    this.fontWeight = FontWeight.w400,
    this.size = 12,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class TitlePage extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const TitlePage({
    this.color = Colors.black,
    this.fontWeight = FontWeight.w600,
    this.size = 16,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class NamaSaham extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const NamaSaham({
    this.color = Colors.black,
    this.fontWeight = FontWeight.w600,
    this.size = 14,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class BoardBeli extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const BoardBeli({
    this.color = Colors.black,
    this.fontWeight = FontWeight.w600,
    this.size = 14,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class Bid extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const Bid({
    this.color = const Color(0xff00aa13),
    this.fontWeight = FontWeight.w600,
    this.size = 14,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class ProfilTitle extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const ProfilTitle({
    this.color = Colors.grey,
    this.fontWeight = FontWeight.w600,
    this.size = 13,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}

class ProfilData extends TextStyle {
  final Color color;
  final FontWeight fontWeight;
  final double size;
  final String fontFamily;

  const ProfilData({
    this.color = Colors.black,
    this.fontWeight = FontWeight.w600,
    this.size = 14,
    this.fontFamily = 'Nunito',
  })  : assert(color != null && fontWeight != null),
        super(
          color: color,
          fontWeight: fontWeight,
          fontSize: size,
          fontFamily: fontFamily,
        );
}
